   apiVersion: v1
   kind: Service
   metadata:
     name: web-server
   spec:
     type: NodePort
     ports:
       - port: 8000
         targetPort: 8000
         nodePort: 30000  # Укажите конкретный порт, если необходимо
     selector:
       app: web-server
   
