apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc-star
  namespace: homework
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-s3